<Window x:Class="WpfMathFormula.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Window1" Height="424" Width="753">
    <Grid>
        <Image Margin="0,0,20,0" Name="image1" Width="{Binding ElementName=scrollBar, Path=Value}" Height="{Binding ElementName=scrollBar, Path=Value}"/>
        <ScrollBar x:Name="scrollBar" Margin="707,0,0,10" Width="38" Minimum="140" Maximum="500" SmallChange="1"/>


    </Grid>
</Window>
